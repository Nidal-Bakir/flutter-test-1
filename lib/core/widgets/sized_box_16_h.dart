import 'package:flutter/material.dart';

class SizedBox16H extends StatelessWidget {
  const SizedBox16H({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 16.0,
    );
  }
}
